name: myapp
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2  # Dependência adicionada corretamente
  expressions: ^0.2.5+2      # Adicionando a biblioteca expressions

dev_dependencies:
  flutter_test:
    sdk: flutter

# Para rodar geradores de código, adicione as seguintes linhas:
# dev_dependencies:
#  build_runner: any
#  json_serializable: any

flutter:
  uses-material-design: true
